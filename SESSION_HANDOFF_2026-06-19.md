# Session Handoff — 2026-06-19 (ADX-бар на фронте + резайз VPS3)

Передача в новую сессию. Контекст: проект **ETH Options Assistant** (`~/Desktop/options`), деплой на **VPS3**.

---

## TL;DR / текущий статус

1. **Диагностика «горит вход, но бот не входит»** — ложная тревога. Бот работал и входил; пользователь смотрел в окно между «загоранием» сигнала и фактическим открытием (лаг ~4–6 мин).
2. **✅ ЗАДАЧА ВЫПОЛНЕНА**: ADX-бар убран из ряда «Близость ко входу» (вариант 3) и **уже в проде**. Доказано: page.tsx изменён 07:37:52 UTC, образ `opt-app-frontend:latest` собран 07:45:02 UTC (после правки), контейнер крутит этот образ; в исходнике `["ADX","adx"]` = 0 раз, массив = `MTF/Vol/Bull`. Сборка/деплой на VPS успели завершиться до того, как резайз и SSH-обрывы всё запутали.
3. **Резайз VPS3 1→2 CPU прошёл** (был временный обрыв всех TCP ~10+ мин — это была переустановка/ребут; box вернулся: 2 CPU, uptime ~2:40, все 7 контейнеров Up). Box, судя по всему, был переустановлен сегодня (см. память о реинстолле; появился новый контейнер `opt-app-bot-1`).
4. **Остаток**: визуально подтвердить на `http://187.127.114.34:3000` (хард-рефреш браузера от кэша) и при желании закоммитить правку в git (сейчас она только в локальном Mac-клоне + на VPS, НЕ закоммичена/не запушена).

---

## Доступы / координаты

- **VPS3**: `ssh root@187.127.114.34`, пароль `B@nd73610421`
- Используем `sshpass -p 'B@nd73610421' ssh -o StrictHostKeyChecking=no root@187.127.114.34 '...'`
- Прод-клон на VPS: **`/root/opt-app`** (source of truth)
- Локальный клон: **`~/Desktop/options`**
- Сервисы: фронт `http://187.127.114.34:3000`, API `http://187.127.114.34:8000`
- `NEXT_PUBLIC_API_URL=http://187.127.114.34:8000/api/v1` (в `/root/opt-app/.env`)
- Контейнеры (docker compose, проект `opt-app`): `opt-app-{paper,frontend,backend,poller,postgres,redis}-1`

---

## Что сделано

### 1. Диагностика бота (paper)
- Все 6 контейнеров были Up, поллер тикает, фронт 200.
- Бот **входил штатно**: открыл серию SELL Call (страйк 1675, экспирация 19JUN26 08:00 UTC, 0.30 ETH, holds 24h, TP1 40%/TP2 80%/SL 75%).
- Логика входа (`backend/services/paper_strategy.py:243`): `ready = vol_high AND regime_ok AND mtf_direction_ok AND bull_filter_ok`. **ADX в гейт НЕ входит** (ADX-сайзинг был отклонён ранее).
- **Bull-фильтр**: `bull_ratio = EMA50(1h)/EMA200(1h)`, порог `bull_market_ratio_max=1.05` для Call (`strategy_config.py:64`), для Put — `None`. Текущий ratio ~1.02 < 1.05 → проходит.
- **Стоп 75%**: `sl_threshold = entry_credit * 1.75` (`paper_loop.py:406`); убыток ≈ −75% от кредита позиции. PnL: `pnl_usd = (entry_credit − exit_debit) × contracts`, кредит хранится в **$/ETH**, контракт 0.3 ETH.

### ⚠️ ВАЖНО: переустановка обнулила paper-историю
После реинстолла/резайза `paper/state` = **equity $400, 0 open, 0 closed, 0W/0L, consec_losses 0**. Вся прежняя статистика (пик $458, 8W/4L, realized +$38.64) и прогресс к go-live gate **СТЁРТЫ** (свежая Postgres-БД). Если история была нужна — проверить, остался ли старый volume/бэкап. Появился новый контейнер `opt-app-bot-1` (новая архитектура бота, развёрнута при реинстолле).

### 2. Итог серии сделок (на момент НАЧАЛА сессии, ДО обнуления)
- Все 4 добранных Call закрылись по **SL** (`exit_counts: sl×4, tp2×8`).
- Equity: $456 пик → **$438.64**; серия SL стоила ≈ **−$19**.
- Статистика: **8W / 4L** (66.7%), maxDD 4.47%, realized +$38.64.
- **⚠️ `consec_losses = 4`, circuit breaker срабатывает на 5** (`CB_CONSEC_LIMIT=5`, пауза 48ч). Ещё один убыток подряд → бот встанет на 2 суток. Это by design.

### 3. Правка фронта (главная задача)
**Что**: на дашборде в блоке «Близость ко входу» ADX-бар был красным даже при «100% ВХОД», т.к. ADX-скор (1.6/10) — это display-индикатор силы, НЕ гейт. Решение пользователя (вариант 3): **убрать ADX из ряда баров, оставить только в верхней строке `ADX 29 · score 1.6/10`**.

**Изменение** — файл `frontend/app/page.tsx` (~строки 193–194):
```diff
- <div className="grid grid-cols-4 gap-2 mt-3">
-   {([["ADX", "adx"], ["MTF", "mtf"], ["Vol", "vol"], ["Bull", "bull"]] as const).map(...
+ <div className="grid grid-cols-3 gap-2 mt-3">
+   {([["MTF", "mtf"], ["Vol", "vol"], ["Bull", "bull"]] as const).map(...
```
- Верхняя строка `ADX … · score …/10` и подпись внизу — оставлены без изменений.
- Локальный `page.tsx` md5 после правки: `f5da5ad29d9bf9dc350d43eb20d2448c`.
- Файл **залит на VPS** (`/root/opt-app/frontend/app/page.tsx`, md5 совпадает, правка подтверждена grep).
- Code-review пройден: 3 бара, типы `as const` валидны, подпись теперь указывает на верхнюю строку.

### 4. Сборка
- **❌ Сборка на Mac под `linux/amd64` НЕ РАБОТАЕТ**: `next build` сегфолтится под QEMU-эмуляцией (`qemu: uncaught target signal 11`) — известная проблема SWC/Next на Apple Silicon. НЕ повторять сборку на Mac без включённого Rosetta в Docker Desktop.
- VPS — `x86_64`. Поэтому **собирали на VPS нативно**:
  ```
  cd /root/opt-app && nohup docker compose build frontend > /tmp/febuild.log 2>&1 < /dev/null &
  ```
- Прогресс до блокера: `npm ci` CACHED → `next build` **✓ Compiled successfully (2.1min)**, **✓ TypeScript прошёл (71s)** ← это тест/ревью, правка типобезопасна → статика собрана → runner-стадия (COPY `.next` DONE, копировал `node_modules`).
- Образ ещё **не финализировался** (тег `opt-app-frontend:latest` оставался старым), когда VPS стал недоступен.

### 5. Резайз → блокер
- Пользователь увеличил VPS3 до **2 CPU** (раньше 1). Это, по-видимому, вызвало reboot/зависание.
- Состояние на паузе: `ping` OK (0% потерь, ~28ms), **SSH:22 CLOSED, :3000 и :8000 = 000** (>10 мин).
- Reboot по SSH невозможен (порт закрыт). Нужен **power-cycle / проверка статуса из панели провайдера**.

---

## TODO в новой сессии (по порядку)

1. **Дождаться возврата VPS**: `nc -z 187.127.114.34 22` → OPEN. При необходимости попросить пользователя сделать Stop→Start в панели провайдера и проверить, что резайз 2 CPU завершён.
2. **Проверить восстановление**: `docker ps` — все 6 контейнеров Up; `nproc` = 2; paper-бот восстановил открытые позиции (`curl -s localhost:8000/api/v1/paper/state` → проверить n_open, consec_losses).
3. **Завершить сборку фронта** (nohup-сборка ребут НЕ переживёт — перезапустить; теперь 2 CPU, нативно ~неск. минут). Запускать детачнуто, лог поллить:
   ```
   cd /root/opt-app && setsid bash -c 'docker compose build frontend > /tmp/febuild.log 2>&1; echo EXIT:$? >> /tmp/febuild.log' &
   ```
4. **Передеплоить фронт** — ОБЯЗАТЕЛЬНО `--force-recreate` (известная грабля: `up -d --no-build` НЕ пересоздаёт контейнер при том же `:latest` теге → останется на старом образе):
   ```
   cd /root/opt-app && docker compose up -d --force-recreate frontend
   ```
5. **Проверить результат** на `http://187.127.114.34:3000`: в блоке «Близость ко входу» осталось **3 бара (MTF / Vol / Bull)**, ADX-бар убран, строка `ADX … · score …/10` сверху на месте.
6. Если ок — по workflow можно закоммитить правку в репозиторий (origin/main был `01856e6`; VPS HEAD `57b6d79`, отстаёт на 1 research-коммит — фронта не касается).

---

## Заметки / грабли

- **Workflow (обязательно)**: architecture → code → review → test → review → deploy. Code-review до деплоя обязателен.
- **VPS = source of truth** для статуса/синка; локальный Mac-клон не авторитетен.
- **Сборка образов**: на Mac amd64-сборка фронта ломается (QEMU segfault). С 2 CPU на VPS нативная сборка фронта стала приемлемой (раньше избегали из-за 1 CPU / >40мин / срыв SSH — это касалось тяжёлых сборок).
- **Next.js 16.2.6 / React 19 / Tailwind v4 + Turbopack** — читать `node_modules/next/dist/docs/` перед правкой фронта (есть `frontend/AGENTS.md` с предупреждением).
- SSH к VPS часто рвётся под нагрузкой (exit 255 / timeout) — это была норма на 1 CPU, с 2 CPU должно полегчать. Запускать тяжёлое через `nohup ... &` + логи в `/tmp`.
- Лог последней сборки на VPS: `/tmp/febuild.log`.
